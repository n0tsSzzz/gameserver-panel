# node_agent (placeholder)

Lightweight FastAPI service that runs on each game node and proxies Docker SDK
calls. Will be created on Stage 3.

This directory is intentionally empty until then — kept as a placeholder so the
intended monorepo layout is visible from day one.
