# worker (placeholder)

ARQ-based async task worker. Will be created on Stage 4 (lifecycle jobs:
`provision_server`, `start`, `stop`, `restart`, `delete`).

This directory is intentionally empty until then — kept as a placeholder so the
intended monorepo layout is visible from day one.
