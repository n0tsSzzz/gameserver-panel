[project]
name = "gamehost-shared"
version = "0.0.0"
description = "Shared enums, schemas, and contracts for GameHost services"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/gamehost_shared"]
